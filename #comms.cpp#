#include <ros/ros.h>

#include <stdio.h>
#include <string.h>

#include <fcntl.h>
#include <errno.h>
#include <termios.h>
#include <unistd.h>

#include "comms.h"

int open_port(char *port) {
  int serial = open(port, O_RDWR);

  if (serial < 0)
    ROS_FATAL("Failed to open port %s with error %i: %s", port, errno, strerror(errno));

  struct termios tty;
  memset(&tty, 0, sizeof tty);

  if(tc
    
}


void write(int serial, char *msg) {

}


void read(int serial, char *buffer) {

}



